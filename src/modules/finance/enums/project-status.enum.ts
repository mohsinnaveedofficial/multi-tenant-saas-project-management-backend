export enum ProjectStatus {
  NOT_STARTED = 'not started',
  IN_PROGRESS = 'in progress',
  ON_HOLD = 'on hold',
  COMPLETED = 'completed',
  CANCELLED = 'cancelled',
  DELAYED = 'delayed',
  UNDER_REVIEW = 'under review',
}